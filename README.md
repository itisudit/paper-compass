# Paper Compass

## Important
This corrected package includes the `assets/` folder required by `index.html`. Upload `index.html` **and the entire `assets/` directory**, preserving paths exactly.

The previous modular archive could leave the homepage blank because the referenced asset files were missing from the uploaded package.

## Current stable structure
- `index.html` — markup and external library tags
- `assets/css/base.css` — complete active stylesheet
- `assets/js/core.js` — complete active application logic

The additional topic files are placeholders for future safe refactoring. Do not delete `base.css` or `core.js`.
